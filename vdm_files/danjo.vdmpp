class Danjo

functions

static public 映画チケット分類関数 : int * int * int-> seq of char
  映画チケット分類関数(age_of_male, age_of_female, ggg) ==
    if (age_of_male + age_of_female <= 50) then
      if(age_of_male >= 18) then
        if(ggg >= 16) then
          "映画チケットは割引価格となる"
        else
          "映画チケットは割引価格とならない。なぜなら、女性が16歳未満だから"
      else
        "映画チケットは割引価格とならない。なぜなら、男性が18歳未満だから"
    else
      "映画チケットは割引価格とならない。なぜなら、男女の歳の合計が50より大きいから";

end Danjo

